import { Route, Routes } from "react-router-dom"
import React, { useEffect, useState } from 'react';
import { userFetch } from "../managers/UserManager.js";
import "./Profile.css"

export const ProfileContainer = () => {
    const [profile, setProfile] = useState([])
}